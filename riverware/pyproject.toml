[project]
name = "riverware"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0",
    "pyarrow>=14.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ipykernel>=6.0",
    "openpyxl>=3.0",
    "matplotlib>=3.7",
]

[tool.uv]
package = false
