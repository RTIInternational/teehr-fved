These data loading notebooks are probably temporary until we get real data flowing.

To use them you must have a TEEHR Cloiud API key and set like:
```
echo TEEHR_DOWNLOAD_API_KEY=YOUR-KEY-HERE > ~/teehr-api-key
```
This key will be read at run time. Do not save your key in these notebooks!